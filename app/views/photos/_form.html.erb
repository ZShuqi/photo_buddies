<%# not used %>
<%= simple_form_for([photo, comment],
                    data: {
                      insert_comment_target: "form",
                      action: "submit->insert-comment#send"
                    }) do |f| %>
  <%= f.input :content, as: :text %>
  <%= f.button :submit, "Send", class: "main-btn" %>
<% end %>
